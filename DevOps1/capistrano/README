#This is for auto application deployment in jenkins Auto Application Deployment job will deploy 
#project code from GITLAB to any VM configured at DESTINATION_IP 

IN capistrano/config/deploy.rb Change the below DETAILS

set :repository, "GITLAB REPO" #GITLAB source code path 
set :deploy_to, "DESTRINATIONFOLDER"#Production VM Destination location
role :app, "DESTINATION_IP"#Production VM ip
 


